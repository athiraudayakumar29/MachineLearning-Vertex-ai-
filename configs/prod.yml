# Prod environment config — values here are placeholders.
# Fill in your real project ID / bucket before use.

DATA_PATH: "gs://YOUR_BUCKET/data/winequality.csv"
MODEL_PATH: "gs://YOUR_BUCKET/models/wine-quality/model.joblib"
MAX_ITER: 1000
RANDOM_STATE: 42
TEST_SIZE: 0.2
MIN_ACCURACY: 0.70
MIN_F1: 0.65

GCP_PROJECT_ID: "your-gcp-project-id"
GCP_REGION: "us-central1"
GCS_BUCKET: "YOUR_BUCKET"
VERTEX_EXPERIMENT_NAME: "wine-quality-training"